const initialState = {
  width: 25,
  height: 25
}

export default function snake(state = initialState, action) {
  // switch (action.type) {
  //   case '':
  //     return { ...state, count: action.payload }
  //   default:
      return state
  }
